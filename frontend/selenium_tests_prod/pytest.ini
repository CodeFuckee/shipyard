[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers -rs
markers =
    prod_smoke: 生产环境只读冒烟测试（不执行任何写操作）
    prod_connect: 生产环境网页授权添加服务器 E2E（写操作，幂等）
